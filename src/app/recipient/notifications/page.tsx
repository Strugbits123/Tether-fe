// Recipient portal — Notifications. Reuses the same UI as the Release Manager
// notifications page; it simply renders within the recipient layout/sidebar.
export { default } from '@/app/rm/notifications/page'
